import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
  component: {
    width: '80%%',
    height: '65vh',
    background: '#fff',
    margin: '80px 140px',
  },
  image: {
    width: '15%',
  },
  container: {
    textAlign: 'center',
    paddingTop: 70,
  },
});
export default useStyle;
